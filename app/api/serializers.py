from pydantic import BaseModel, Field

class RoomSerializer(BaseModel):
    # Le faire ;)
    pass

class PlayerSerializer(BaseModel): # Changer le base model en le truc natif qui fait des serializers (méthodes natives)
    # Le faire ;)
    pass
